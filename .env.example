# Client (Vite) config. Copy to .env.local for local dev.

# Point the client at the API server to enable Mongo-backed storage + login.
# Leave UNSET (or empty) to use the default localStorage-only demo mode.
# VITE_API_URL=http://localhost:4000
